{
  "name": "marketmesh-full",
  "private": true,
  "version": "1.0.0",
  "description": "MarketMesh full stack (backend + frontend) runner",
  "scripts": {
    "start": "concurrently -k -n BACKEND,FRONTEND -c green,cyan \"npm --prefix backend start\" \"npm --prefix frontend start\"",
    "start:backend": "npm --prefix backend start",
    "start:frontend": "npm --prefix frontend start",
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "seed": "npm --prefix backend run seed"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
